import TextElement from '../../sharedcomponents/TextElement'

const AdditionalTool = ({
    toolName
}) => {
    return (
        <TextElement textValue={toolName} />
    )
}

export default AdditionalTool